/**
 * COPILOT TASK (STEP 1C):
 * Projects page:
 * - Fetch projects from mock API getProjects()
 * - Show project cards (name, key, description)
 * - Button opens Create Project modal
 * - Create Project form: name, key (auto-gen), description
 * - On submit: createProject(payload) then refresh list
 * - Add loading and empty states
 */
