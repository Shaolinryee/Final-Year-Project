/**
 * COPILOT TASK (STEP 1D):
 * Kanban board page:
 * - Use projectId from params
 * - Fetch tasks via getProjectTasks(projectId)
 * - Group tasks into columns TODO, IN_PROGRESS, DONE
 * - Render task cards
 * - Create Task modal adds task into TODO
 * - Clicking a task opens a right-side drawer (placeholder is fine)
 */
